string str = "\0";
for(int i=0 ; i < nodes.size() ; i++)
{
    str += nodes[i];
    str += " -> ";
    for(int j=0; j < adjacenylist[i].getlength() ; j = j->getnext()){
        str += adjacenylists[j];
        str += " , ";
    }
    str.erase(str.end()-2 , str.end());
    str += "\n";
}